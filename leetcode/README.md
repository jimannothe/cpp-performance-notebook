# LeetCode (Performance Lens)

For each problem, record:

- Time complexity
- Memory complexity
- Access pattern (sequential/random)
- Branch predictability risks
- Allocation count (explicit + implicit)
- Alternative with better locality

Use this filename format:

`<id>-<slug>.md` and `<id>-<slug>.cpp`
